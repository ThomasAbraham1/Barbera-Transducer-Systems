services:
  - type: static
    name: barbera-frontend
    env: static
    rootDirectory: frontend
    buildCommand: npm run build
    publishDir: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
